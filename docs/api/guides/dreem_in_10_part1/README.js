Ext.data.JsonP.dreem_in_10_part1({"guide":"<h1 id='dreem_in_10_part1-section-dreemgl-in-10-minutes'>DreemGL in 10 Minutes</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ul>\n<li>1. <a href='#!/guide/dreem_in_10_part1-section-the-dreemgl-toolkit'>The DreemGL Toolkit</a>\n <ul>\n<li>1.1. <a href='#!/guide/dreem_in_10_part1-section-system-requirements'>System Requirements</a>\n </li>\n<li>1.2. <a href='#!/guide/dreem_in_10_part1-section-installing-dreem'>Installing Dreem</a>\n </li>\n<li>1.3. <a href='#!/guide/dreem_in_10_part1-section-launching-the-dreemgl-server'>Launching the DreemGL server</a>\n </li>\n</ul></li>\n<li>2. <a href='#!/guide/dreem_in_10_part1-section-dreemgl---core-concepts'>DreemGL - Core concepts</a>\n </li>\n<li>3. <a href='#!/guide/dreem_in_10_part1-section-dreemgl-%27hello-world%27'>DreemGL 'Hello World'</a>\n <ul>\n<li>3.1. <a href='#!/guide/dreem_in_10_part1-section-compositions-and-uris'>Compositions and URIs</a>\n </li>\n<li>3.2. <a href='#!/guide/dreem_in_10_part1-section-%27hello-world%27-composition'>'Hello World' composition</a>\n </li>\n<li>3.3. <a href='#!/guide/dreem_in_10_part1-section-the-screen-class'>The Screen Class</a>\n </li>\n<li>3.4. <a href='#!/guide/dreem_in_10_part1-section-classes-in-dreemgl'>Classes in DreemGL</a>\n </li>\n<li>3.5. <a href='#!/guide/dreem_in_10_part1-section-the-view-class---baseclass-of-all-visible-objects-in-dreem'>The View Class - Baseclass of all visible objects in Dreem</a>\n </li>\n<li>3.6. <a href='#!/guide/dreem_in_10_part1-section-class-initialization'>Class initialization</a>\n </li>\n</ul></li>\n<li>4. <a href='#!/guide/dreem_in_10_part1-section-rendering-in-dreem'>Rendering in Dreem</a>\n <ul>\n<li>4.1. <a href='#!/guide/dreem_in_10_part1-section-the-render%28%29-function'>The render() function</a>\n </li>\n</ul></li>\n<li>5. <a href='#!/guide/dreem_in_10_part1-section-attributes'>Attributes</a>\n <ul>\n<li>5.1. <a href='#!/guide/dreem_in_10_part1-section-creating-attributes'>Creating Attributes</a>\n </li>\n</ul></li>\n<li>6. <a href='#!/guide/dreem_in_10_part1-section-service-class-and-the-rpc-system'>Service Class and the RPC System</a>\n <ul>\n<li>6.1. <a href='#!/guide/dreem_in_10_part1-section-binding-to-server-side-attributes'>Binding to server-side attributes</a>\n </li>\n</ul></li>\n</ul></div>\n\n<p>DreemGL is a GL (webGL) based UI toolkit and IOT system written in JavaScript. In DreemGL Shaders replace CSS and are written in type inferenced JS,\nlarge data can be rendered using vertexbuffers, the DOM is created React-style, and multiple devices/screens share one 'composition' space with automated RPC system.</p>\n\n<p>This introduction gets you familiar with the DreemGL toolkit, and how you can use it create multi-screen experiences with it. You'll become familiar with the structure of a <em>multi-screen application</em> or <em>composition</em>, how we call them in Dreem. You will get to know the classes and APIs for creating UIs, learn how to add interactive elements, we will show you how to load data, and pass data between server and clients.</p>\n\n<h2 id='dreem_in_10_part1-section-the-dreemgl-toolkit'>The DreemGL Toolkit</h2>\n\n<p>DreemGL is a toolkit for prototyping GPU-accelereated multi-screen experiences with IoT integration for smart environments. DreemGL simplifies the prototyping of applications that connect large screens in the environment to multiple handheld devices, tablets, or IoT smart objects.</p>\n\n<p>The UI on all screens can take advantage of GPU capabilities, enabling more fluid and subtle animations on mobile GPUs, matching video game level graphics performance. DreemGL applications are written in pure JavaScript, the The Lingua Franca of the Web.</p>\n\n<h3 id='dreem_in_10_part1-section-system-requirements'>System Requirements</h3>\n\n<h4 id='dreem_in_10_part1-section-required-software'>Required Software</h4>\n\n<ul>\n<li>Node.js version 4.2.6 or higher <a href=\"https://nodejs.org/en/download/\">https://nodejs.org/en/download/</a>.</li>\n<li>Git client (command line <a href=\"https://git-scm.com/downloads\">https://git-scm.com/downloads</a> or <a href=\"https://desktop.github.com/\">Github desktop application</a> for Windows or Mac)</li>\n<li>Web browser with webGL support (See your favorite browser for support)</li>\n</ul>\n\n\n<h4 id='dreem_in_10_part1-section-teem-server---supported-operating-systems'>Teem Server - Supported Operating Systems</h4>\n\n<p>Running the DreemGL server component - the Node.js based Teem server - is supported on the following platforms:\n - Windows 7, Windows 8.1\n - Mac OS X 10.7\n - Linux (tested on Ubuntu Linux 14.04, should work on other distrutions)</p>\n\n<h4 id='dreem_in_10_part1-section-webgl-runtime---os-%26amp%3B-browser-combinations'>WebGL Runtime - OS &amp; Browser Combinations</h4>\n\n<p>DreemGL applications using the WebGL runtime can be run with the following operating &amp; browser combinations:</p>\n\n<table>\n<thead>\n<tr>\n<th> OS </th>\n<th> OS Version </th>\n<th> Browser </th>\n<th> Browser Version </th>\n<th> Notes </th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td> Windows </td>\n<td> 7, 8.1, (10) </td>\n<td> Chrome </td>\n<td> 45+ </td>\n<td> Not testing on Windows 10, but should work. </td>\n</tr>\n<tr>\n<td>         </td>\n<td> 7, 8.1, (10) </td>\n<td> Firefox </td>\n<td> 42+ </td>\n<td> Not testing on Windows 10, but should work. </td>\n</tr>\n<tr>\n<td>         </td>\n<td> 10 </td>\n<td> Edge </td>\n<td>  </td>\n<td> Supports WebGL, but still some issues with DreemGL </td>\n</tr>\n<tr>\n<td> OS X </td>\n<td> 10.7+ </td>\n<td> Safari </td>\n<td> 9+ </td>\n<td> </td>\n</tr>\n<tr>\n<td>          </td>\n<td>       </td>\n<td> Firefox </td>\n<td> 42+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td>          </td>\n<td>       </td>\n<td> Chrome </td>\n<td> 23+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td> Linux </td>\n<td> Any recent distrubution </td>\n<td> Safari </td>\n<td> 23+ </td>\n<td> Tested on Ubuntu 14.04 </td>\n</tr>\n<tr>\n<td>          </td>\n<td>       </td>\n<td> Chrome </td>\n<td> 45+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td>          </td>\n<td>       </td>\n<td> Firefox </td>\n<td> 42+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td> iOS </td>\n<td> 8+ </td>\n<td> Safari </td>\n<td> 8.4, 9+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td> Android </td>\n<td>  </td>\n<td> Chrome </td>\n<td> 25+ </td>\n<td> Depends on device/GPU driver support for WebGL. Tested on Nexus 5, Galaxy S6, Galaxy Note 5, NVidia Shield Tablet. </td>\n</tr>\n</tbody>\n</table>\n\n\n<h3 id='dreem_in_10_part1-section-installing-dreem'>Installing Dreem</h3>\n\n<p>For now, the best way to install DreemGL on your computer is to check out the source code from Github, using the following URL:\nhttps://github.com/dreemproject/dreemgl</p>\n\n<p>This is the link to the master branch of Dreem, which should be stable. The branch which is used for development is the DreemGL dev branch:\nhttps://github.com/dreemproject/dreemgl/tree/dev</p>\n\n<p><em>Please do not use the dev branch for development, unless you are using a specific feature, which is not available in master, yet. There will be many breaking changes for existing compositions going into a dev branch!</em></p>\n\n<h3 id='dreem_in_10_part1-section-launching-the-dreemgl-server'>Launching the DreemGL server</h3>\n\n<p>You can launch the DreemGL server by running</p>\n\n<pre><code class=\"bash\">$ node server.js\nServer running on http://127.0.0.1:2000/\n</code></pre>\n\n<p>To test if everything is working fine, open the following URL for the <em>treeart2.js</em> composition in a supported browser:\n<a href=\"/examples/treeart2\" target=\"_blank\" data-example=\"Animated shader tree demo|580|500\">http://localhost:2000/examples/treeart2</a></p>\n\n<p>You should be seeing an animated tree with some nice shader effects:</p>\n\n<p><img src=\"https://raw.githubusercontent.com/dreemproject/dreemgl/dev/docs/images/treeart.png\" width=\"581\" height=\"500\"/></p>\n\n<p>There are a number of command line flags for the server, the most important ones being the <code>-iface</code> <code>-port</code> and the <code>-path</code> flags:</p>\n\n<pre><code class=\"bash\">&gt; node server.js --help\nTeem Server 2.0\ncommandline: node server.js &lt;flags&gt;\n-port [port] Server port\n-nomoni  Start process without monitor\n-iface [interface] Server interface\n-restart Auto restarts after crash (Handy for client dev, not server dev)\n-path [name]:[directory] add a path to the server under name $name\n</code></pre>\n\n<p>By default the server is running on 127.0.0.1 with port 2000. If you want to access the server from other IP addresses or devices on your local network, you have to launch it with the <code>-iface 0.0.0.0</code> option. This way it will bind to your local IP address:</p>\n\n<pre><code class=\"bash\">$ node server.js -iface 0.0.0.0\nServer running on http://127.0.0.1:2000/ on http://192.168.0.14:2000/ Ready to go!\n</code></pre>\n\n<p>This will bind the DreemGL server to the IP address(es) of your network card (can be multiple, e.g. if you are using a VPN connection). All IP addresses will be listed, as you can see above. In our case the IP address was <em>192.168.0.14</em>, but for your machine it will be different. If you want to connect to a composition from one or more of your devices, for the example above the URI for the treeart2 demo would be <em>http://192.168.0.14:2000/examples/treeart2</em>.</p>\n\n<p>The port can be set using the <code>-port</code> option, e.g.</p>\n\n<pre><code class=\"bash\">$ node server.js -iface 0.0.0.0 -port 3555\nServer running on http://127.0.0.1:3555/ on http://192.168.0.14:3555/ Ready to go!\n</code></pre>\n\n<p>Throughout the guide you will see that we are using the host and port <em>localhost:2000</em>, but the hyperlinks are relative, so they will work with your own IP address.</p>\n\n<h2 id='dreem_in_10_part1-section-dreemgl---core-concepts'>DreemGL - Core concepts</h2>\n\n<p>DreemGL uses the concept of a <code>composition</code>. The composition acts as a container for all source elements of a multi-screen experience. The core elements of a composition are:</p>\n\n<ul>\n<li><strong>composition</strong> The composition context contains all global objects, constants, and properties which are used across screens and servers for that specific composition. You create a composition by subclassing the composition.</li>\n<li><strong>screen:</strong> A <code>screen</code> corresponds to a physical device with a screen, e.g. a TV screen, smartphone screen, smartwatch screen, screen of an IoT device, or a desktop PC's screen. This is often a webbrowser. A composition can have any number of different screens for different devices or activities. Screens best correspond to the concept of a mobile or TV app in the traditional application development model.</li>\n<li><strong>service:</strong> Base class for all server-side dreemGL components.</li>\n<li><strong>view:</strong> Base class for all visual child nodes of screen</li>\n</ul>\n\n\n<p><strong><em>Todo: Add a screenshot of a composition architecture, showing DreemGL server running with multiple screens connected, the hub or bus, as well as IoT devices connected.</em></strong></p>\n\n<h4 id='dreem_in_10_part1-section-power-of-the-composition-based-approach'>Power of the composition based approach</h4>\n\n<p>There are a number of reasons why we chose to use a composition based approach instead of an app based approach (single app per screen device):</p>\n\n<ul>\n<li><strong>Central file for all screens:</strong>\nA composition bundles all the code for the whole multi-screen experience:\n\n<ul>\n<li>JavaScript classes, which can be run on both server and client.</li>\n<li>Screens or physical displays: All screens for devices with displays are part of the composition.</li>\n</ul>\n</li>\n<li><strong>Share code between client and server:</strong> JavaScript classes can be run both on the server (Node.js environment), and in the browser. There is no need to create separate implementation of the functionality for client or server side.</li>\n<li><strong>Run on the GPU, but code in JavaScript:</strong> The whole UI renders in GL, but developers do not need to use the syntax of <code>GLSL</code> (the <code>OpenGL Shading Language</code>, a high-level shading language based on the syntax of the C programming language) but can comfortably stay in JS.</li>\n<li><strong>Multi-runtime support:</strong>DreemGL has been architectured to support multiple runtimes, where the runtimes can share a large portion of the DreemGL runtime code. The default runtime is the <em>WebGL runtime</em>, which renders in browsers. There is also basic support for Tizen OS runtime based on the DALi 3d toolkit with the Node.js add-on, a feauture which is called the <em>DALi runtime</em>. DALi runtime applications can be run on Ubuntu 14.04 with the latest versions of the DALi toolkit pre-compiled, or on Hardkernel's Odroid XU4 development board, the reference devices for the Tizen 3.0 TV profile.</li>\n</ul>\n\n\n<h2 id='dreem_in_10_part1-section-dreemgl-%27hello-world%27'>DreemGL 'Hello World'</h2>\n\n<p>Before we dive into the source code of our <em>Hello World</em> composition, you need to understand how the server maps compositions to URIs.</p>\n\n<h3 id='dreem_in_10_part1-section-compositions-and-uris'>Compositions and URIs</h3>\n\n<p>You create a composition in its own file. When running a composition, the server maps the composition's JavaScript file to a URI. Remember the <em>treeart2.js</em> composition? The URI for the composition is\n<a href=\"/examples/treeart2\" target=\"_blank\" data-example=\"Animated shader tree demo|400|400\">http://localhost:2000/examples/treeart2</a></p>\n\n<p>You will find the corresponding file at <code>DREEM/examples/treeart2</code>, where <code>DREEM</code> is the root folder of your DreemGL installation. While you can add your own compositions to the DREEM/examples, the better approach is to put your all your own compositions into a separate folder (e.g. a folder on the same level as the DreemGL toolkit folder). You can make your own composition folder available to DreemGL by using the <code>-path</code> option when launching the server, e.g.</p>\n\n<pre><code class=\"bash\">node server.js -path project1:../myproject\n</code></pre>\n\n<p>Here <em>project1</em> is the virtual server path assigned to the <em>myproject</em> folder. If you create a composition named <em>helloworld.js</em> in this folder, you can run the composition by loading:</p>\n\n<p><a href=\"/project1/helloworld\" target=\"_blank\" data-example=\"Hello world composition|400|400\">http://localhost:2000/project1/helloworld</a></p>\n\n<p>As you can see, the file ending <code>.js</code> is omitted when running a composition.</p>\n\n<h3 id='dreem_in_10_part1-section-%27hello-world%27-composition'>'Hello World' composition</h3>\n\n<p>Here is our first composition. It doesn't do much, just renders a single screen with a green background color. But we use it explain the general syntax of classes and compositions.</p>\n\n<p><strong>helloworld.js <a href=\"/docs/examples/dreem10/helloworld\" data-example=\"Hello world composition|400|400\">Open composition in new tab</a></strong></p>\n\n<pre><code class=\"javascript\">define.class(\"$server/composition\",\n  function (\n    // imports from classes/ui folder\n    $ui$, screen\n  ) {\n    this.render = function() {\n      var s = screen({name:'default',clearcolor: 'green'});\n      return[s];\n    };\n  }\n);\n</code></pre>\n\n<p>All compositions need to subclass the <em>$server/composition</em> class. The syntax for creating a class or subclassing an existing class in DreemGL is:</p>\n\n<pre><code class=\"javascript\">define.class(\"$server/composition\", function() {});\n</code></pre>\n\n<p><code>$server</code> is a <em>default path symbol</em>, which resolves to a default path or folder in the DreemGL framework. The <em>default path symbols</em> are use in class definitions. The following table shows an overview all default paths you can use:</p>\n\n<table>\n<thead>\n<tr>\n<th> Path Symbol </th>\n<th> DreemGL Folder </th>\n<th> Description </th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td> <code>$behaviors</code> </td>\n<td> <a href=\"../examples\">$root/classes/behaviors</a></td>\n<td> Behaviors (e.g. drag support)  </td>\n</tr>\n<tr>\n<td> <code>$examples</code> </td>\n<td> <a href=\"../examples\">$root/examples</a></td>\n<td> DreemGL example compositions </td>\n</tr>\n<tr>\n<td> <code>$resources</code> </td>\n<td> <a href=\"../resources\">$root/resources</a></td>\n<td> Fonts, icons, and textures. </td>\n</tr>\n<tr>\n<td> <code>$server</code> </td>\n<td> <a href=\"../classes/server\">$root/classes/server</a></td>\n<td> Composition, dataset, IO, services. </td>\n</tr>\n<tr>\n<td> <code>$system</code> </td>\n<td> <a href=\"../system\">$root/system</a></td>\n<td> System classes like geometry, shader support, runtimes, RPC classes. </td>\n</tr>\n<tr>\n<td> <code>$testing</code> </td>\n<td> <a href=\"../classes/testing\">$root/classes/testing</a></td>\n<td> Classes used for testing.  </td>\n</tr>\n<tr>\n<td> <code>$ui</code> </td>\n<td> <a href=\"../classes/ui\">$root/classes/ui</a></td>\n<td> Core UI components. </td>\n</tr>\n<tr>\n<td> <code>$widgets</code> </td>\n<td> <a href=\"../classes/widgets\">$root/system</a></td>\n<td> UI widgets used by applications, e.g. colorpicker, searchbox, radiogroup. </td>\n</tr>\n<tr>\n<td> <code>$3d</code> </td>\n<td> <a href=\"../classes/3d\">$root/classes/3d</a></td>\n<td> System classes for 3d support in DreemGL </td>\n</tr>\n</tbody>\n</table>\n\n\n<p>DreemGL uses an <a href=\"https://github.com/amdjs/amdjs-api/blob/master/AMD.md\">Asynchronous Module Definition (AMD)</a> based style of defining modules and internally works a lot like require.js. The loader is called define.js (after the define global it creates)\nIn define.js classes are a first class citizen of the module system, which is used in the live reloading and the nested classes which the shaders are using.\nThe name of the class is also handily used for the constructor so debugging with browser devtools is nicer.</p>\n\n<p>So back to the DreemGL class definition. The two key concepts you need to be aware of here are the idea of a <code>define.class</code> method for facilitating module definition and a require mechanism for handling dependency loading.</p>\n\n<p>Take a look at the signature of the structure for class definitions below:</p>\n\n<pre><code class=\"javascript\">define.class(\n  superclass,           // superclass is referenced using $folder/classname syntax\n  function($ui$, view)  // requiring of dependencies, like class imports from packages in other languages\n    {\n      ...               // class body\n  }\n);\n</code></pre>\n\n<p>The first argument to define class is a string containing a path symbol (<code>$server</code> in this case), and the classname of the superclass: <code>composition</code>. The file name of the superclass is <code>composition.js</code>, and the file can be found in the folder <code>DREEM/system/server</code>. In Dreem, the file name of the class file is automatically used as the classname. This limits the names you can use for your class files, since the name has to be valid JavaScript identifier, with some limitations.</p>\n\n<p>A JavaScript identifier must start with a letter, underscore (_), or dollar sign ($) but don't use this one; subsequent characters can also be digits (0-9). Because JavaScript is case sensitive, letters include the characters \"A\" through \"Z\" (uppercase) and the characters \"a\" through \"z\" (lowercase).</p>\n\n<p>For a DreemGL class name there are additional limitations:</p>\n\n<ul>\n<li>Only a single underscore is allowed at the beginning of a class name.</li>\n<li>Don't use <code>$</code> in file names, since it has a special functionality in class looks or requires.</li>\n<li>If you want to separate lexical units in composition names, it's recommended to use <code>_</code> (underscore) instead of <code>-</code> (dash), so <code>hello_world.js</code> instead of <code>hello-world.js</code>, spaces and - characters are replaced with '_' by the class name generator</li>\n</ul>\n\n\n<h3 id='dreem_in_10_part1-section-the-screen-class'>The Screen Class</h3>\n\n<p>For a composition to be able to render anything visible (or a UI) there needs to be at least one screen object as a direct child of the composition. DreemGL uses a very specific and efficient rendering mechanism, which makes us of the <code>render()</code> function:</p>\n\n<pre><code class=\"javascript\">this.render = function () {\n  return [ screen({name: 'default', clearcolor: \"grey\"}) ];\n}\n</code></pre>\n\n<p>The render function returns an array of views (or view subclasses), which will rendered as direct children of the current class. The following code instantiates a screen object, and sets the attribute <code>name</code> to <code>default</code>, and the <code>clearcolor</code> attribute to <code>grey</code>.</p>\n\n<p>The <code>new</code> keyword is not used, and should be avoided - since it breaks the styling system when used on classes.</p>\n\n<pre><code class=\"javascript\">return [ screen({name: 'default', clearcolor: \"grey\"}) ];\n</code></pre>\n\n<p>Compositions can have multiple screens, and each screen needs to have name attribute with a unique name.\nWe will have a more detailed look at the <code>render()</code> function in a bit, but you should first learn how to define classes in Dreem.</p>\n\n<p><strong>docs/examples/dreem10/helloworld_multiscreen.js</strong> <a href=\"/docs/examples/dreem10/helloworld_multiscreen\" data-example=\"Multiscreen composition|400|400\">open composition in new tab</a></p>\n\n<pre><code class=\"javascript\">// composition with two screens\ndefine.class(\"$server/composition\",\n  function (\n    // imports from classes/ui folder\n    $ui$, screen, label\n  ) {\n    this.render = function() {\n      return[\n        screen(\n                    {name:'default',clearcolor: 'coolgrey'},\n                    label({fontsize: 30, text: \"default screen\"})\n                    ),\n        screen(\n                    {name:'mobile',clearcolor: 'brilliantazure'},\n                    label({fontsize: 30, text: \"mobile screen\"})\n                )\n      ];\n    };\n  }\n);\n</code></pre>\n\n<p>With more than one screen in a composition, DreemGL will render the first screen child by default. To access another screen, you have to append the screen name directly to the query string (directly behind the question mark). The screen with name <code>mobile</code> can therefore be accessed by using the screen name as the only value of the query string (no key/value pair, just the name of the screen directly following the question mark).\n<a href=\"/docs/examples/dreem10/helloworld_multiscreen?mobile\" target=\"_blank\" data-example=\"helloworld_multiscreen?mobile|400|400\">http://localhost:2000/docs/examples/dreem10/helloworld_multiscreen?mobile</a></p>\n\n<p>For the default screen, just remove the <code>?mobile</code> query string, and DreemGL will load the default screen:\n<a href=\"/docs/examples/dreem10/helloworld_multiscreen\" target=\"_blank\" data-example=\"helloworld_multiscreen|400|400\">http://localhost:2000/docs/examples/dreem10/helloworld_multiscreen</a></p>\n\n<h3 id='dreem_in_10_part1-section-classes-in-dreemgl'>Classes in DreemGL</h3>\n\n<p>Let's have another look at class definitions. As you have already learned, DreemGL classes are defined in a single file. Classes use the same define syntax as compositions. Take a look at the following code sample:</p>\n\n<pre><code class=\"javascript\">define.class('$ui/view', function(require, exports, $ui$, label) {\n    var mylib = require('./mylib')\n\n    this.method = function(){\n    }\n\n    exports.staticmethod = function(){\n    }\n})\n</code></pre>\n\n<p>You can see the the same class definition construct as we had earlier in our composition. This code sample uses a different formatting style, where the class definition with superclass and imports is on a single line.</p>\n\n<p>The first argument to define.class is the <em>require</em> function, which sets the base class: <code>$ui/view</code>. The second argument is the class body function. The arguments of this function act as imports of classes used by this specific class.</p>\n\n<p>When requiring or importing classes DreemGL provides a number of shortcuts to folders, which are listed below. The shortcuts are called <em>directory switches</em>, which include all commonly folders with classes in the toolkit.</p>\n\n<table>\n<thead>\n<tr>\n<th> Path Symbol </th>\n<th> DreemGL Folder </th>\n<th> Description </th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td> <code>$examples$</code> </td>\n<td> <a href=\"../examples\">$root/examples</a></td>\n<td> DreemGL example compositions </td>\n</tr>\n<tr>\n<td> <code>$server$</code> </td>\n<td> <a href=\"../classes/server\">$root/classes/server</a></td>\n<td> Composition, dataset, IO, services. </td>\n</tr>\n<tr>\n<td> <code>$system$</code> </td>\n<td> <a href=\"../system\">$root/system</a></td>\n<td> System classes like geometry, shader support, runtimes, RPC classes. </td>\n</tr>\n<tr>\n<td> <code>$ui$</code> </td>\n<td> <a href=\"../classes/ui\">$root/classes/ui</a></td>\n<td> Core UI components. </td>\n</tr>\n<tr>\n<td> <code>$widgets$</code> </td>\n<td> <a href=\"../classes/widgets\">$root/system</a></td>\n<td> UI widgets used by applications, e.g. colorpicker, searchbox, radiogroup. </td>\n</tr>\n<tr>\n<td> <code>$3d$</code> </td>\n<td> <a href=\"../classes/3d\">$root/classes/3d</a></td>\n<td> System classes for 3d support in DreemGL </td>\n</tr>\n<tr>\n<td> <code>$$</code> </td>\n<td> current directory </td>\n<td> Used to import classes relative to current directory. </td>\n</tr>\n<tr>\n<td> <code>relative$dir$</code> </td>\n<td> relative directory</td>\n<td> Directory relative to current or parent directory. </td>\n</tr>\n</tbody>\n</table>\n\n\n<ul>\n<li><code>require</code> and <code>exports</code> are special cases: They can be placed anywhere in the argument list, where they appear does not matter.</li>\n<li><code>exports</code> is the class constructor function, and can hold static methods.</li>\n<li> <code>require</code> is simply the local instance of require if needed for normal requires.</li>\n<li><code>$ui$</code> switches directory in the dependency-class list, in this case to the folder <code>DREEM/classes/ui</code>, from where the <code>label.js</code> class is imported.</li>\n</ul>\n\n\n<p>The body of the function (the 2nd argument passed to define.class) is the class body, where you can attributes, add your own functions/methods to the class.</p>\n\n<p>The following example app consists of a composition named <code>require_external_class.js</code>, which imports the <code>simplebox.js</code> class. The <code>simplebox</code> class does not provide much functionality, it is just a rectangle with a default orange background color.</p>\n\n<p>The composition imports the class <code>simplebox.js</code>. The directory switch <code>$$</code> with the following <code>simplebox</code> means: Import the class <code>simplebox.js</code> from the same folder as the composition. You can list any number of classes from a directory following the directory switch. As long as you don't use another directory switch, all classes will be imported based on the folder from last directory switch used.</p>\n\n<p><strong>Example: Requiring an external class</strong> - <a href='/docs/examples/dreem10/require_external_class' target='_blank' data-example=\"require_external_class|400|400\">click to run</a></p>\n\n<pre><code class=\"javascript\">// Composition showing import of class simplebox.js from the same directory\ndefine.class(\"$server/composition\",\n    function (\n        $ui$, screen,  // imports from classes/ui folder\n        $$, simplebox  // import custom class\n    ) {\n        this.render = function() {\n            return[\n                screen(\n                    {name:'normal', bgcolor: 'battleshipgrey'},\n                    simplebox({x: 50, y: 20, w:200, h:100})\n                )\n            ];\n        };\n    }\n);\n</code></pre>\n\n<p>The <code>$$, simplebox</code> way of importing a class is just a shortcut, you can always require an external class or library using <code>this.myclass = require('./myclass.js')</code>, so the above composition could be written like this:</p>\n\n<pre><code class=\"javascript\">// Composition showing import of class simplebox.js using require\ndefine.class(\"$server/composition\",\n    function (require, $ui$, screen) {\n        this.simplebox = require('./simplebox.js');\n        this.render = function() {\n            return[\n                screen(\n                    {name:'normal', bgcolor: 'battleshipgrey'},\n                    this.simplebox({x: 10, y:10}),\n                    this.simplebox({x: 100, y: 50, w:100, h:60, bgcolor: 'celestialblue'})\n                )\n            ];\n        };\n    }\n);\n</code></pre>\n\n<p>When using <code>require</code> to import a class, the require module itself must be imported before it can be used. As mentioned earlier, the <code>require</code> module is a special case, since it can be listed at any position inside the class body function arguments.</p>\n\n<p>Let's take a closer look at both ways of importing:</p>\n\n<pre><code class=\"javascript\">define.class(\"$server/composition\", function (require, $ui$, screen) {\n    this.simplebox = require('./simplebox.js');\n...\n</code></pre>\n\n<p>And the short version:</p>\n\n<pre><code class=\"javascript\">define.class(\"$server/composition\", function ($ui$, screen, $$, simplebox) {\n...\n</code></pre>\n\n<p>As you can see, the class body function arguments for the first version contain <em>require</em> as the first argument. The require module is then used to load the class <em>simplebox.js</em>. For the short version, DreemGL takes care of converting the imports into requires.</p>\n\n<p>Finally, here is the class which is being imported, the <code>simplebox.js</code>. It subclasses view, and sets default values for absolute positioning.</p>\n\n<pre><code class=\"javascript\">// simple view with default w/h and bgcolor; used by require_external_class.js\ndefine.class('$ui/view',\n    function() {\n        this.position = 'absolute';\n        this.w = 120;\n        this.h = 80;\n        this.bgcolor = 'fluorescentorange';\n        this.init = function() {\n            console.log(\"class init event\")\n        }\n    }\n);\n</code></pre>\n\n<h3 id='dreem_in_10_part1-section-the-view-class---baseclass-of-all-visible-objects-in-dreem'>The View Class - Baseclass of all visible objects in Dreem</h3>\n\n<p>The view.js class is the baseclass of all visible items on screen. It contains all attributes that are used by the render system  to layout, and draw a view. A view has a set of <code>children</code>. The child views of a view using the <code>this.children</code> reference. Each view owns a set of shaders that it iterates over to draw them.</p>\n\n<p><strong>Example: Requiring an external class</strong> - <a href='/docs/examples/dreem10/view_children' target='_blank' data-example=\"view_children|400|400\">click to run</a></p>\n\n<pre><code class=\"javascript\">define.class(\"$server/composition\", function ($ui$, screen, view, label) {\n    this.render = function() {\n      return[\n        screen( { name:'normal', clearcolor: 'white' }\n          ,view({\n              name: 'container', w: 305, bgcolor: 'battleshipgrey',\n              oninit: function() {\n                for (var i=0; i&lt;this.children.length; i++) {\n                  console.log(\"child #\" + i + \": name=\" + this.children[ i ].name);\n                  this.children[ i ].text = \"view #\" + i;\n                }\n              }\n            }\n            ,label({ name: 'v1', w: 80, h:30, margin: 10, bgcolor: 'amaranthred' })\n            ,label({ name: 'v2', w: 80, h:30, margin: 10, bgcolor: 'castletongreen' })\n            ,label({ name: 'v3', w: 80, h:30, margin: 10, bgcolor: 'amber' })\n          )\n        )\n      ];\n    };\n  }\n);\n</code></pre>\n\n<p>In the above example the screen has exactly one child, the view with <code>name='container'</code>. That view contains three subviews: The labels with the name v1, v2, and v2. We defined an <code>oninit</code> function in the view, which iterates over the <code>this.children</code> array, and sets the text attribute on each label to <code>view #x</code> where <code>x</code> is the index nub.</p>\n\n<h3 id='dreem_in_10_part1-section-class-initialization'>Class initialization</h3>\n\n<p>In the DreemGL classes, you'll find a number of so called <code>at</code> functions, e.g. <code>atConstructor</code>, <code>atRender</code>, etc.\nDuring the initialization process of a class, the following functions are called in this order:</p>\n\n<ol>\n<li><code>atConstructor()</code> is called when you construct the class, generally not used by views: use init</li>\n<li><code>init()</code> is called to initialize the class, create things here.</li>\n<li><code>render()</code> is called after <code>init()</code> and gives the view the opportunity to return its child list.</li>\n</ol>\n\n\n<p>The following class shows the functions in exactly the order they are called at. <code>render()</code> and <code>atRender()</code> can be called multiple times, if any attributes of the class change, which are used within render.</p>\n\n<pre><code class=\"javascript\">define.class('$ui/view',\n  function() {\n    this.atConstructor = function() {\n      console.log('class_init_test#atConstructor()');\n    };\n\n    this.init = function() {\n      console.log('class_init_test#init()');\n    };\n\n    this.render = function() {\n      console.log('class_init_test#render()');\n      return [];\n    }\n  }\n);\n</code></pre>\n\n<h2 id='dreem_in_10_part1-section-rendering-in-dreem'>Rendering in Dreem</h2>\n\n<p>As a framework rendering to a GL device, DreemGL does not render the UI by modifying a DOM. The DreemGL application is rendered to a WebGL context inside a HTML5 canvas.</p>\n\n<h3 id='dreem_in_10_part1-section-the-render%28%29-function'>The render() function</h3>\n\n<p>The <code>render()</code> function in DreemGL is the function that provides the children of a view node, and it automatically re-executes when any of the view's attributes you ‘reference’ in that render function change externally.</p>\n\n<p>The use of the <code>render()</code> function provides a functional way to define state through the render structure. Instead of building and managing a tree structure, attributes are mapped directly to a render tree: There is no separation between init and update, all code is basically init code. When something in the structure changes, a new set of children is returned by the render function. The difference between Facebook's React framework's approach and Dreem's approach is that in React's case the render process returns a new tree or subtree, which will be attached to the virtual DOM tree. In Dreem, the render function can only return children, since there is no DOM or virtual DOM tree to work with.</p>\n\n<pre><code class=\"javascript\">define.class('$ui/view',\n  function() {\n    this.atConstructor = function() {\n      console.log('class_init_test#atConstructor()');\n    };\n\n    this.init = function() {\n      console.log('class_init_test#init()');\n    };\n\n    this.render = function() {\n      console.log('class_init_test#render()');\n      return [\n        view({w: 100, h: 100, bgcolor: 'red'})\n      ];\n    }\n  }\n);\n</code></pre>\n\n<h2 id='dreem_in_10_part1-section-attributes'>Attributes</h2>\n\n<p><strong>Attributes</strong> are special properties on an object. Inside the class body function of a DreemGL class, you can define attributes like this:</p>\n\n<pre><code class=\"javascript\">this.attributes = {\n    aBool: false,\n    numColor: 0,\n    someText: \"Just a string\",\n    colors: [],\n    moreText: Config(type:string, value:\"Another string\"),\n    joined: Config({value: false, type: Boolean}),\n    userid: Config({value: -1, type: Number}),\n    moreColor: Config({type: Array})\n    candidate: Config({type: Object, value: {}, persist:true}),\n};\n</code></pre>\n\n<p>As you can see, there is a short syntax available, listing just the attribute name and value.</p>\n\n<p>Attributes come with a powerful, built-in API to allow other parts of your code to bind to value of changes of your attributes.</p>\n\n<h3 id='dreem_in_10_part1-section-creating-attributes'>Creating Attributes</h3>\n\n<p>To create attributes, define a magical attribute setter as shown in <a href=\"https://github.com/dreemproject/dreemgl/blob/dev/system/base/node.js\">node.js</a>.</p>\n\n<p><code>this.attributes = {}</code> is actually a function call. Using setters as\ninit calls allows DreemGL to create nested json and assign them to\nclasses all at once: <code>{attributes:{}}</code></p>\n\n<h2 id='dreem_in_10_part1-section-service-class-and-the-rpc-system'>Service Class and the RPC System</h2>\n\n<p>DreemGL has a powerful WebSocket based RPC system, which makes it extremely easy to share data across multiple screens using and between server and client. To write code running on the Node.js server, you can simple extend the <code>&lt;a href='http://localhost:2000/docs/api/index.html#!/api/service' target='_blank'&gt;service&lt;/a&gt;</code> class.</p>\n\n<h3 id='dreem_in_10_part1-section-binding-to-server-side-attributes'>Binding to server-side attributes</h3>\n\n<p>The <code>service</code> class can be treated like any other class in DreemGL, where you can add attributes and methods to the class. This RPC system then exposes the service and all the attributes to all screens in your composition. Let's start with a small example:</p>\n\n<p><a href=\"/docs/examples/dreem10/service_colorview_example\" target=\"_blank\" data-example=\"service_colorview|400|400\">/docs/examples/dreem10/service_colorview</a></p>\n\n<pre><code class=\"javascript\">// service example with embedded classes for service and view\ndefine.class(\"$server/composition\",\n    function ($server$, service, $ui$, screen, view) {\n\n        // Embedded service class which sends a random set of pos/size values\n        // to the client.\n        define.class(this, \"boxrandomizer\", \"$server/service\", function(){\n\n            this.attributes = {\n                viewprops1: Config({ type:Array, value: [] }),\n                viewprops2: Config({ type:Array, value: [] }),\n                colors: Config({type: Array,\n                    value: ['uclagold', 'utahcrimson', 'richelectricblue', 'willpowerorange', 'upsdellred', 'yelloworange', 'seagreen']}),\n                color1: Config({ type:String, value:'black' }),\n                color2: Config({ type:String, value:'black' })\n            }\n            this.randinterval = null;\n\n            // Set up the interval for calling randomizeBoxDims\n            this.oninit = function(){\n                this.randinterval = this.setInterval(this.randomizeViewProps, 2500);\n            }\n\n            // Generates a set of view dimensions and two random color values\n            this.randomizeViewProps = function() {\n                var rand = this.randomInt;\n                this.viewprops1 = vec4(rand(1, 100), rand(1, 100), rand(100, 200), rand(100, 200));\n                this.viewprops2 = vec4(rand(1, 100), rand(1, 100), rand(100, 200), rand(100, 200));\n                this.color1 = this.colors[rand(1, this.colors.length-1)]\n                this.color2 = this.colors[rand(1, this.colors.length-1)]\n            }\n\n            // Generate a random int value for a range\n            this.randomInt = function(min, max) {\n                if (max == null) {\n                    max = min;\n                    min = 0;\n                }\n                return min + Math.floor(Math.random() * (max - min + 1));\n            };\n\n        });\n\n        // View class which will receive the props from boxrandomizer service\n        define.class(this, \"colorview\", view, function() {\n\n            this.borderradius = vec4(10);\n\n            this.attributes = {\n              dimensions: Config({value: vec4(0), type:vec4}),\n                newx: Config({type:int, value:0}),\n                newy: Config({type:int, value:0}),\n                neww: Config({type:int, value:0}),\n                newh: Config({type:int, value:0})\n            };\n\n            this.onnewx = function() {\n                this.x = this.newx\n            }\n            this.onnewy = function() {\n                this.y = this.newy\n            }\n            this.onneww = function() {\n                this.w = this.neww\n            }\n            this.onnewh = function() {\n                this.h = this.newh\n            }\n\n            this.ondimensions = function() {\n                var dims = this.dimensions;\n                this.newx = Config({value:dims[0], motion:\"inoutquad\", duration:.75})\n                this.newy = Config({value:dims[1], motion:\"inoutquad\", duration:.95})\n                this.neww = Config({value:dims[2], motion:\"inoutquad\", duration:1.1})\n                this.newh = Config({value:dims[3], motion:\"inoutquad\", duration:.8})\n            }\n\n        })\n\n        this.render = function() {\n            var s = screen();\n            return[\n                this.boxrandomizer({name:'boxrandom'}),\n                screen(\n                    {name:'default',clearcolor: 'onyx'},\n                    // The position and bgcolor of these two colorviews will be set through the\n                    // boxrandomizer service's attributes.\n                    this.colorview({\n                        name: 'v1',\n                        position: 'absolute',\n                        dimensions: wire(\"this.rpc.boxrandom.viewprops1\"),\n                        bgcolor: wire(\"this.rpc.boxrandom.color1\"),\n                        opacity: 0.6\n                    }),\n                    this.colorview({\n                        name: 'v2',\n                        position: 'absolute',\n                        dimensions: wire(\"this.rpc.boxrandom.viewprops2\"),\n                        bgcolor: wire(\"this.rpc.boxrandom.color2\"),\n                        opacity: 0.4\n                    })\n\n                )\n            ];\n        };\n    }\n);\n</code></pre>\n\n<iframe name='docrunner' style=\"width:1px; height:1px; border:0\" src=\"/docs/examples/docexamplerunner\"></iframe>\n\n","title":"DreemGL in 10 Minutes"});