Ext.data.JsonP.dreem_in_10_part1({"guide":"<!---\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ul>\n<li>1. <a href='#!/guide/dreem_in_10_part1-section-the-dreemgl-toolkit'>The DreemGL Toolkit</a>\n <ul>\n<li>1.1. <a href='#!/guide/dreem_in_10_part1-section-system-requirements'>System Requirements</a>\n </li>\n<li>1.2. <a href='#!/guide/dreem_in_10_part1-section-installing-dreem'>Installing Dreem</a>\n </li>\n<li>1.3. <a href='#!/guide/dreem_in_10_part1-section-launching-the-dreemgl-server'>Launching the DreemGL server</a>\n </li>\n</ul></li>\n<li>2. <a href='#!/guide/dreem_in_10_part1-section-dreemgl---core-concepts'>DreemGL - Core concepts</a>\n </li>\n<li>3. <a href='#!/guide/dreem_in_10_part1-section-dreemgl-%27hello-world%27'>DreemGL 'Hello World'</a>\n <ul>\n<li>3.1. <a href='#!/guide/dreem_in_10_part1-section-compositions-and-uris'>Compositions and URIs</a>\n </li>\n<li>3.2. <a href='#!/guide/dreem_in_10_part1-section-%27hello-world%27-composition'>'Hello World' composition</a>\n </li>\n<li>3.3. <a href='#!/guide/dreem_in_10_part1-section-classes'>Classes</a>\n </li>\n<li>3.4. <a href='#!/guide/dreem_in_10_part1-section-class-initialization'>Class initialization</a>\n </li>\n</ul></li>\n<li>4. <a href='#!/guide/dreem_in_10_part1-section-rendering-in-dreem'>Rendering in Dreem</a>\n <ul>\n<li>4.1. <a href='#!/guide/dreem_in_10_part1-section-the-render%28%29-function'>The render() function</a>\n </li>\n<li>4.2. <a href='#!/guide/dreem_in_10_part1-section-the-view-class---baseclass-of-all-visible-objects-in-dreem'>The View Class - Baseclass of all visible objects in Dreem</a>\n </li>\n</ul></li>\n</ul></div>\n   Copyright 2015-2016 Teem. Licensed under the Apache License, Version 2.0 (the \"License\"); Dreem is a collaboration between Teem & Samsung Electronics, sponsored by Samsung.\n   You may not use this file except in compliance with the License. You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0\n   Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on an \"AS IS\" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND,\n   either express or implied. See the License for the specific language governing permissions and limitations under the License.\n-->\n\n\n<h1 id='dreem_in_10_part1-section-dreemgl-in-10-minutes---part-1'>DreemGL in 10 Minutes - Part 1</h1>\n\n<p>This introduction gets you familiar with the DreemGL toolkit, and how you can use it create multi-screen experiences with it. You'll become familiar with the structure of a <em>multi-screen application</em> or <em>composition</em>, how we call them in Dreem. You will get to know the classes and APIs for creating UIs, learn how to add interactive elements, we will show you how to load data, and pass data between server and clients.</p>\n\n<h2 id='dreem_in_10_part1-section-the-dreemgl-toolkit'>The DreemGL Toolkit</h2>\n\n<p>DreemGL is a toolkit for prototyping GPU-accelereated multi-screen experiences with IoT integration for smart environments. DreemGL simplifies the prototyping of applications that connect large screens in the environment to multiple handheld devices, tablets, or IoT smart objects.</p>\n\n<p>The UI on all screens can take advantage of GPU capabilities, enabling more fluid and subtle animations on mobile GPUs, matching video game level graphics performance. DreemGL applications are written in pure JavaScript, the The Lingua Franca of the Web.</p>\n\n<h3 id='dreem_in_10_part1-section-system-requirements'>System Requirements</h3>\n\n<h4 id='dreem_in_10_part1-section-required-software'>Required Software</h4>\n\n<ul>\n<li>Recent version of Node.js, which can be downloaded <a href=\"https://nodejs.org/en/download/\">here</a>.</li>\n<li>Git client (command line or <a href=\"https://desktop.github.com/\">desktop application</a>)</li>\n</ul>\n\n\n<h4 id='dreem_in_10_part1-section-teem-server---supported-operating-systems'>Teem Server - Supported Operating Systems</h4>\n\n<p>Running the DreemGL server component - the Node.js based Teem server - is supported on the following platforms:\n - Windows 7, Windows 8.1\n - Mac OS X 10.7\n - Linux (tested on Ubuntu Linux 14.04, should work on other distrutions)</p>\n\n<h4 id='dreem_in_10_part1-section-webgl-runtime---os-%26amp%3B-browser-combinations'>WebGL Runtime - OS &amp; Browser Combinations</h4>\n\n<p>DreemGL applications using the WebGL runtime can be run with the following operating &amp; browser combinations:</p>\n\n<table>\n<thead>\n<tr>\n<th> OS </th>\n<th> OS Version </th>\n<th> Browser </th>\n<th> Browser Version </th>\n<th> Notes </th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td> Windows </td>\n<td> 7, 8.1, (10) </td>\n<td> Chrome </td>\n<td> 45+ </td>\n<td> Not testing on Windows 10, but should work. </td>\n</tr>\n<tr>\n<td>         </td>\n<td> 7, 8.1, (10) </td>\n<td> Firefox </td>\n<td> 42+ </td>\n<td> Not testing on Windows 10, but should work. </td>\n</tr>\n<tr>\n<td>         </td>\n<td> 10 </td>\n<td> Edge </td>\n<td>  </td>\n<td> Supports WebGL, but still some issues with DreemGL </td>\n</tr>\n<tr>\n<td> OS X </td>\n<td> 10.7+ </td>\n<td> Safari </td>\n<td> 9+ </td>\n<td> </td>\n</tr>\n<tr>\n<td>          </td>\n<td>       </td>\n<td> Firefox </td>\n<td> 42+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td>          </td>\n<td>       </td>\n<td> Chrome </td>\n<td> 23+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td> Linux </td>\n<td> Any recent distrubution </td>\n<td> Safari </td>\n<td> 23+ </td>\n<td> Tested on Ubuntu 14.04 </td>\n</tr>\n<tr>\n<td>          </td>\n<td>       </td>\n<td> Chrome </td>\n<td> 45+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td>          </td>\n<td>       </td>\n<td> Firefox </td>\n<td> 42+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td> iOS </td>\n<td> 8+ </td>\n<td> Safari </td>\n<td> 8.4, 9+ </td>\n<td>  </td>\n</tr>\n<tr>\n<td> Android </td>\n<td>  </td>\n<td> Chrome </td>\n<td> 25+ </td>\n<td> Depends on device/GPU driver support for WebGL. Tested on Nexus 5, Galaxy S6, Galaxy Note 5, NVidia Shield Tablet. </td>\n</tr>\n</tbody>\n</table>\n\n\n<h3 id='dreem_in_10_part1-section-installing-dreem'>Installing Dreem</h3>\n\n<p>For now, the best way to install DreemGL on your computer is to check out the source code from Github, using the following URL:\nhttps://github.com/teem2/dreemgl</p>\n\n<p>This is the link to the master branch of Dreem, which should be stable. The branch which is used for development is the DreemGL dev branch:\nhttps://github.com/teem2/dreemgl/tree/dev</p>\n\n<p><em>Please do not use the dev branch for development, unless you are using a specific feature, which is not available in master, yet. There will be many breaking changes for existing compositions going into a dev branch!</em></p>\n\n<h3 id='dreem_in_10_part1-section-launching-the-dreemgl-server'>Launching the DreemGL server</h3>\n\n<p>You can launch the DreemGL server by running</p>\n\n<pre><code class=\"bash\">node server.js\n</code></pre>\n\n<p>By default the server is running on port 2000, but you can configure a different port using the <code>-port</code> option, e.g.</p>\n\n<pre><code class=\"bash\">node server.js -port 3555\n</code></pre>\n\n<p>To test if everything is working fine, open the following URL for the <em>shadertest.js</em> composition in a supported browser:\n<a href=\"http://localhost:2000/examples/shadertest\" target=\"_blank\">http://localhost:2000/examples/shadertest</a></p>\n\n<p>You should be seeing an animated shape rendering a shaders, similar to the image below:\n<img src=\"../../images/shadertest.png\" width=\"500\"/></p>\n\n<p>There are a number of command line flags for the server, the most important ones being the <code>-port</code> and the <code>-path</code> flags: ds</p>\n\n<pre><code class=\"bash\">&gt; node server.js --help\nTeem Server 2.0\ncommandline: node server.js &lt;flags&gt;\n-port [port] Server port\n-nomoni  Start process without monitor\n-iface [interface] Server interface\n-browser Opens webbrowser on default app\n-notify Shows errors in system notification\n-devtools Automatically opens devtools in the browser\n-close Auto closes your tab when reloading the server\n-delay Delay reloads your pages when reloading the server\n-restart Auto restarts after crash (Handy for client dev, not server dev)\n-edit Automatically open an exception in your code editor at the right line\n-path [name]:[directory] add a path to the server under name $name\n</code></pre>\n\n<h2 id='dreem_in_10_part1-section-dreemgl---core-concepts'>DreemGL - Core concepts</h2>\n\n<p>DreemGL uses the concept of a <code>composition</code>. The composition acts as a container for all source elements of a multi-screen experience. The core elements of a composition are:</p>\n\n<ul>\n<li><strong>Composition</strong> The composition context contains all global objects, constants, and properties which are used across screens and servers for that specific composition. You create a composition by subclassing the composition.</li>\n<li><strong>Server:</strong> The server object inside a composition. Each composition can contain one instance of a server object.</li>\n<li><strong>Screen:</strong> A <code>screen</code> corresponds to a physical display, e.g. a TV screen, smartphone screen, smartwatch screen, screen of an IoT device, or a desktop PC's screen. A composition can have a unlimited number of screens (TODO: Is there a limit for the number of screens per composition?). Screens best correspond to the concept of a mobile or TV app in the traditional application development model.</li>\n<li><strong>Teem Hub or Bus:</strong> Bundles the RPC APIs and features for communication betweeen different screens, devices, and the server.</li>\n</ul>\n\n\n<p><strong><em>Todo: Add a screenshot of a composition architecture, showing DreemGL server running with multiple screens connected, the hub or bus, as well as IoT devices connected.</em></strong></p>\n\n<h4 id='dreem_in_10_part1-section-power-of-the-composition-based-approach'>Power of the composition based approach</h4>\n\n<p>There are a number of reasons why we chose to use a composition based approach instead of an app based approach (single app per screen device):</p>\n\n<ul>\n<li><strong>Central file for all screens:</strong>\nA composition bundles all the code for the whole multi-screen experience:\n\n<ul>\n<li>JavaScript classes, which can be run on both server and client.</li>\n<li>Screens or physical displays: All screens for devices with displays are part of the composition.</li>\n</ul>\n</li>\n<li><strong>Share code between client and server:</strong> JavaScript classes can be run both on the server (Node.js environment), and in the browser. There is no need to create separate implementation of the functionality for client or server side.</li>\n<li><strong>Run on the GPU, but code in JavaScript:</strong> The whole UI renders in GL, but developers do not need to learn <code>GLSL</code> (the <code>OpenGL Shading Language</code>, a high-level shading language based on the syntax of the C programming language).</li>\n<li><strong>Multi-runtime support:</strong>DreemGL has been architectured to support multiple runtimes, where the runtimes can share a large portion of the DreemGL runtime code. The default runtime is the <em>WebGL runtime</em>, which renders in browsers. There is also basic support for Tizen OS runtime based on the DALi 3d toolkit with the Node.js add-on, a feauture which is called the <em>DALi runtime</em>. DALi runtime applications can be run on Ubuntu 14.04 with the latest versions of the DALi toolkit pre-compiled, or on Hardkernel's Odroid XU4 development board, the reference devices for the Tizen 3.0 TV profile.</li>\n</ul>\n\n\n<h2 id='dreem_in_10_part1-section-dreemgl-%27hello-world%27'>DreemGL 'Hello World'</h2>\n\n<p>Before we dive into the source code of our <em>Hello World</em> composition, you need to understand how the server maps compositions to URIs.</p>\n\n<h3 id='dreem_in_10_part1-section-compositions-and-uris'>Compositions and URIs</h3>\n\n<p>You create a composition in its own file. When running a composition, the server maps the composition's JavaScript file to a URI. Remember the <em>shadertest.js</em> composition? The URI for the composition is\n<a href=\"http://localhost:2000/examples/shadertest\" target=\"_blank\">http://localhost:2000/examples/shadertest</a></p>\n\n<p>You will find the corresponding file at <code>DREEM/examples/shadertest.js</code>, where <code>DREEM</code> is the root folder of your DreemGL installation. While you can add your own compositions to the DREEM/examples, the better approach is to put your all your own compositions into a separate folder (e.g. a folder on the same level as the DreemGL toolkit folder). You can make your own composition folder available to DreemGL by using the <code>-path</code> option when launching the server, e.g.</p>\n\n<pre><code class=\"bash\">node server.js -path project1:../myproject\n</code></pre>\n\n<p>Here <em>project1</em> is the virtual server path assigned to the <em>myproject</em> folder. If you create a composition named <em>helloworld.js</em> in this folder, you can run the composition by loading:\n<a href=\"http://localhost:2000/examples/shadertest\" target=\"_blank\">http://localhost:2000/project1/helloworld</a></p>\n\n<p>As you can see, the file ending <code>.js</code> is omitted when running a composition.</p>\n\n<h3 id='dreem_in_10_part1-section-%27hello-world%27-composition'>'Hello World' composition</h3>\n\n<p>Here is our first composition. It doesn't do much, just renders a single screen with a green background color. But we use it explain the general syntax of classes and compositions.</p>\n\n<p><strong>helloworld.js <a href=\"http://localhost:2000/guide/examples/helloworld\">(open composition in new tab)</a></strong></p>\n\n<pre><code class=\"javascript\">define.class(\"$server/composition\",\n  function (\n    // imports from classes/ui folder\n    $ui$, screen\n  ) {\n    this.render = function() {\n      var s = screen({name:'default',clearcolor: 'green'});\n      return[s];\n    };\n  }\n);\n</code></pre>\n\n<p>All compositions need to subclass the <em>$server/composition</em> class. The syntax for creating a class or subclassing an existing class in DreemGL is:\n<code>javascript\ndefine.class(\"$server/composition\", function() {});\n</code>\n<code>$server</code> is a <em>default path symbol</em>, which resolves to a default path or folder in the DreemGL framework. The <em>default path symbols</em> are use in class definitions. The following table shows an overview all default paths you can use:</p>\n\n<table>\n<thead>\n<tr>\n<th> Path Symbol </th>\n<th> DreemGL Folder </th>\n<th> Description </th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td> <code>$behaviors</code> </td>\n<td> <a href=\"../examples\">$root/classes/behaviors</a></td>\n<td> Behaviors (e.g. drag support)  </td>\n</tr>\n<tr>\n<td> <code>$examples</code> </td>\n<td> <a href=\"../examples\">$root/examples</a></td>\n<td> DreemGL example compositions </td>\n</tr>\n<tr>\n<td> <code>$resources</code> </td>\n<td> <a href=\"../resources\">$root/resources</a></td>\n<td> Fonts, icons, and textures. </td>\n</tr>\n<tr>\n<td> <code>$server</code> </td>\n<td> <a href=\"../classes/server\">$root/classes/server</a></td>\n<td> Composition, dataset, IO, services. </td>\n</tr>\n<tr>\n<td> <code>$system</code> </td>\n<td> <a href=\"../system\">$root/system</a></td>\n<td> System classes like geometry, shader support, runtimes, RPC classes. </td>\n</tr>\n<tr>\n<td> <code>$testing</code> </td>\n<td> <a href=\"../classes/testing\">$root/classes/testing</a></td>\n<td> Classes used for testing.  </td>\n</tr>\n<tr>\n<td> <code>$ui</code> </td>\n<td> <a href=\"../classes/ui\">$root/classes/ui</a></td>\n<td> Core UI components. </td>\n</tr>\n<tr>\n<td> <code>$widgets</code> </td>\n<td> <a href=\"../classes/widgets\">$root/system</a></td>\n<td> UI widgets used by applications, e.g. colorpicker, searchbox, radiogroup. </td>\n</tr>\n<tr>\n<td> <code>$3d</code> </td>\n<td> <a href=\"../classes/3d\">$root/classes/3d</a></td>\n<td> System classes for 3d support in DreemGL </td>\n</tr>\n</tbody>\n</table>\n\n\n<p>DreemGL uses an <a href=\"https://github.com/amdjs/amdjs-api/blob/master/AMD.md\">Asynchronous Module Definition (AMD)</a> based style of defining classes. The Asynchronous Module Definition is a JavaScript specification of an API for defining code modules and their dependencies, and loading them asynchronously if desired.</p>\n\n<p>Implementations of AMD provide the following benefits:</p>\n\n<ul>\n<li>AMD implementations allow developers to encapsulate code in smaller, more logically-organized files in a way similar to other programming languages such as Java.</li>\n<li>Website performance improvements. AMD implementations load smaller JavaScript files, and only load them when they're needed.</li>\n<li>Fewer page errors. AMD implementations allow developers to define dependencies that must load before a module is executed, so the module doesn't try to use outside code that isn't yet available.</li>\n</ul>\n\n\n<p>So back to the DreemGL class definition. The two key concepts you need to be aware of here are the idea of a <code>define.class</code> method for facilitating module definition and a require mechanism for handling dependency loading.</p>\n\n<p>Take a look at the signature of the structure for class definitions below:</p>\n\n<pre><code class=\"javascript\">define.class(\n  superclass,          /* superclass is referenced using $folder/classname syntax */\n  definition function  /* function for instantiating the module or object */\n    ($ui$, view)        /* requiring of dependencies, like class imports from packages in other languages */\n    {\n      ...              /* class body */\n  }\n);\n</code></pre>\n\n<p>The first argument to define class is a string containing a path symbol (<code>$server</code> in this case), and the classname of the superclass: <code>composition</code>. The file name of the superclass is <code>composition.js</code>, and the file can be found in the folder <code>DREEM/system/server</code>. In Dreem, the file name of the class file is automatically used as the classname. This limits the names you can use for your class files, since the name has to be valid JavaScript identifier, with some limitations.</p>\n\n<p>A JavaScript identifier must start with a letter, underscore (_), or dollar sign ($); subsequent characters can also be digits (0-9). Because JavaScript is case sensitive, letters include the characters \"A\" through \"Z\" (uppercase) and the characters \"a\" through \"z\" (lowercase).</p>\n\n<p>For a DreemGL class name there are additional limitations:</p>\n\n<ul>\n<li>Only a single underscore is allowed at the beginning of a class name.</li>\n<li>Calls names can not start with <code>$</code>.</li>\n<li>If you want to separate lexical units in composition names, remember to use <code>_</code> (underscore) instead of <code>-</code> (dash), so <code>hello_world.js</code> instead of <code>hello-world.js</code>, which would result in an error.</li>\n</ul>\n\n\n<p>For a composition to be able to render anything visible (or a UI) there needs to be at least one screen object as a direct child of the composition. DreemGL uses a very specific and efficient rendering mechanism, which makes us of the <code>render()</code> function:</p>\n\n<pre><code class=\"javascript\">this.render = function () {\n  return [ screen({name: 'default', clearcolor: \"grey\"}) ];\n}\n</code></pre>\n\n<p>The render function returns an array of views (or view subclasses), which will rendered as direct children of the current class. The following code instantiates a screen object, and sets the attribute <code>name</code> to <code>default</code>, and the <code>clearcolor</code> attribute to <code>grey</code>.</p>\n\n<p>The <code>new</code> keyword is not required, but can be used, so feel free to use this syntax, which is a more explicit form of instantiation, if you prefer this.</p>\n\n<pre><code class=\"javascript\">return [ screen({name: 'default', clearcolor: \"grey\"}) ];\n</code></pre>\n\n<p>Compositions can have multiple screens, and each screen needs to have name attribute with a unique name.\nWe will have a more detailed look at the <code>render()</code> function in a bit, but you should first learn how to define classes in Dreem.</p>\n\n<p><strong>helloworld_multiscreen.js <a href=\"http://localhost:2000/guide/examples/helloworld_multiscreen\">(open composition in new tab)</a></strong></p>\n\n<pre><code class=\"javascript\">define.class(\"$server/composition\",\n  function (\n    // imports from classes/ui folder\n    $ui$, screen\n  ) {\n    this.render = function() {\n      return[\n        screen({name:'default',clearcolor: 'bostonuniversityred'}),\n        screen({name:'mobile',clearcolor: 'brilliantazure'})\n      ];\n    };\n  }\n);\n</code></pre>\n\n<p>With more than one screen in a composition, DreemGL will render the first screen child by default. To access another screen, you have to append the screen name directly to the query string (directly behind the question mark). The screen with name <code>mobile</code> can therefore be accessed using the URI:\n<a href=\"http://localhost:2000/guide/examples/helloworld_multiscreen?mobile\" target=\"_blank\">http://localhost:2000/guide/examples/helloworld_multiscreen?mobile</a></p>\n\n<h3 id='dreem_in_10_part1-section-classes'>Classes</h3>\n\n<p>Let's have another look at class definitions. As you have already learned, DreemGL classes are defined in a single file. Classes use the same define syntax as compositions. Take a look at the following code sample:</p>\n\n<pre><code class=\"javascript\">define.class('$ui/view', function(require, exports, $ui$, label) {\n    var mylib = require('./mylib')\n\n    this.method = function(){\n    }\n\n    exports.staticmethod = function(){\n    }\n})\n</code></pre>\n\n<p>You can see the the same class definition construct as we had earlier in our composition. This code sample uses a different formatting style, where the class definition with superclass and imports is on a single line.</p>\n\n<p>The first argument to define.class is the <em>require</em> function, which sets the base class: <code>$ui/view</code>. The following function has a list of arguments, which are the imports for the class.</p>\n\n<ul>\n<li><code>require</code> and <code>exports</code> are special cases: They can be placed anywhere in the argument list, where they appear does not matter.</li>\n<li><code>exports</code> is the class constructor function, and can hold static methods.</li>\n<li> <code>require</code> is simply the local instance of require if needed for normal requires.</li>\n<li><code>$ui$</code> switches directory in the dependency-class list, in this case to the folder <code>DREEM/classes/ui</code>, from where the <code>label.js</code> class is imported.</li>\n</ul>\n\n\n<p>There are a number of predefined directory switches:</p>\n\n<table>\n<thead>\n<tr>\n<th> Path Symbol </th>\n<th> DreemGL Folder </th>\n<th> Description </th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td> <code>$examples$</code> </td>\n<td> <a href=\"../examples\">$root/examples</a></td>\n<td> DreemGL example compositions </td>\n</tr>\n<tr>\n<td> <code>$server$</code> </td>\n<td> <a href=\"../classes/server\">$root/classes/server</a></td>\n<td> Composition, dataset, IO, services. </td>\n</tr>\n<tr>\n<td> <code>$system$</code> </td>\n<td> <a href=\"../system\">$root/system</a></td>\n<td> System classes like geometry, shader support, runtimes, RPC classes. </td>\n</tr>\n<tr>\n<td> <code>$ui$</code> </td>\n<td> <a href=\"../classes/ui\">$root/classes/ui</a></td>\n<td> Core UI components. </td>\n</tr>\n<tr>\n<td> <code>$widgets$</code> </td>\n<td> <a href=\"../classes/widgets\">$root/system</a></td>\n<td> UI widgets used by applications, e.g. colorpicker, searchbox, radiogroup. </td>\n</tr>\n<tr>\n<td> <code>$3d$</code> </td>\n<td> <a href=\"../classes/3d\">$root/classes/3d</a></td>\n<td> System classes for 3d support in DreemGL </td>\n</tr>\n<tr>\n<td> <code>$$</code> </td>\n<td> current directory </td>\n<td> Used to import classes relative to current directory. </td>\n</tr>\n<tr>\n<td> <code>$$$</code> </td>\n<td> parent directory </td>\n<td> Used to import classes from parent directory or subfolder of parent directory. </td>\n</tr>\n<tr>\n<td> <code>relative$dir$</code> </td>\n<td> relative directory</td>\n<td> Directory relative to current or parent directory. </td>\n</tr>\n</tbody>\n</table>\n\n\n<p>The body of the function (the 2nd argument passed to define.class) is the class body, where you can attributes, add your own functions/methods to the class.</p>\n\n<p>The following example app consists of a composition named <code>class1.js</code>, which imports the <code>simplebox.js</code> class. The <code>simplebox</code> class does not provide much functionality, it shows a rectangle with a background color.</p>\n\n<p><strong>Class example: simplebox.js </strong>\n<a href='/guides/examples/classes/class1.js' target='_blank'>Run example in new tab</a></p>\n\n<pre><code class=\"javascript\">define.class('$ui/view',\n  function() {\n    this.w = 200;\n    this.h = 100;\n    this.bgcolor = 'green';\n    this.init = function() {\n      console.log(\"class init event\")\n    }\n  }\n);\n</code></pre>\n\n<p>The composition import the class <code>simplebox.js</code>. The directory switch <code>$$</code> with the following <code>simplebox</code> means: Import the class <code>simplebox.js</code> from the same folder as the composition.</p>\n\n<pre><code class=\"javascript\">define.class(\"$server/composition\",\n  function (\n    $ui$, screen,  // imports from classes/ui folder\n    $$, simplebox  // import custom class\n  ) {\n    this.render = function() {\n      return[\n        screen(\n          {name:'default'},\n          simplebox()\n        )\n      ];\n    };\n  }\n);\n</code></pre>\n\n<h3 id='dreem_in_10_part1-section-class-initialization'>Class initialization</h3>\n\n<p>In the DreemGL classes, you'll find a number of so called <code>at</code> functions, e.g. <code>atConstructor</code>, <code>atRender</code>, etc.\nDuring the initialization process of a class, the following functions are called in this order:\n 1. <code>_atConstructor()</code> is called before that if you want to put important things in your baseclass and you dont want your users to have to call it manually\n 2. <code>atConstructor()</code> is called when you construct the class\n 3. <code>init()</code> is called once the class has been initialized.\n 4. <code>render()</code> is called after <code>init()</code>.\n 5. <code>atRender()</code>is called directly after render. We use it for example to add scrollbars to a view.</p>\n\n<p>The following class shows the functions in exactly the order they are called at. <code>render()</code> and <code>atRender()</code> can be called multiple times, if any attributes of the class change, which are used within render.</p>\n\n<pre><code class=\"javascript\">define.class('$ui/view',\n  function() {\n    this._atConstructor = function() {\n      console.log('class_init_test#_atConstructor()');\n    };\n\n    this.atConstructor = function() {\n      console.log('class_init_test#atConstructor()');\n    };\n\n    this.init = function() {\n      console.log('class_init_test#init()');\n    };\n\n    this.render = function() {\n      console.log('class_init_test#render()');\n      return [];\n    }\n\n    this.atRender = function() {\n      console.log('class_init_test#atRender()');\n    };\n  }\n);\n</code></pre>\n\n<p>There is a similarly named <code>doRender()</code> function on the composition, which is an internal API (and should be renamed at some point in the future).</p>\n\n<h2 id='dreem_in_10_part1-section-rendering-in-dreem'>Rendering in Dreem</h2>\n\n<p>As a framework rendering to a GL device, DreemGL does not render the UI by modifying a DOM. The DreemGL application is rendered to a WebGL context inside a HTML5 canvas.</p>\n\n<h3 id='dreem_in_10_part1-section-the-render%28%29-function'>The render() function</h3>\n\n<p>The <code>render()</code> function in DreemGL is the function that provides the children of a view node, and it automatically re-executes when any of the view's attributes you ‘reference’ in that render function change externally.</p>\n\n<p>The use of the <code>render()</code> function provides a functional way to define state through the render structure. Instead of building and managing a tree structure, attributes are mapped directly to a render tree: There is no separation between init and update, all code is basically init code. When something in the structure changes, a new set of children is returned by the render function. The difference between Facebook's React framework's approach and Dreem's approach is that in React's case the render process returns a new tree or subtree, which will be attached to the virtual DOM tree. In Dreem, the render function can only return children, since there is no DOM or virtual DOM tree to work with.</p>\n\n<h3 id='dreem_in_10_part1-section-the-view-class---baseclass-of-all-visible-objects-in-dreem'>The View Class - Baseclass of all visible objects in Dreem</h3>\n\n<p>The view.js class is the baseclass of all visible items on screen. It contains all attributes that are used by the render system  to layout, and draw a view. A view has a set of <code>children</code>. You can access the child views of a view using the <code>this.children</code> reference. Each view owns a set of shaders that it iterates over to draw them.</p>\n","title":"Dreem in 10 - Part 1"});